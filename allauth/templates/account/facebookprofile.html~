<html>
<head>
                          <title><center>Your Facebook Account Profile</br></center></title>
</head>
<body onLoad="FB.logout();">	

<!-- reference to google's jquery ui library -->
<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js"></script>

<!-- this div element is required...I have no idea why...but when i remove it, the log out doesn't occur -->
<div id="fb-root">
</div>

<!-- pull down Facebook's Javascript SDK -->
<script src="http://connect.facebook.net/en_US/all.js"></script>

<!-- execute the following script immediately when the page loads -->
<script>
// initialize the library with your Facebook API key
FB.init({ apiKey:424576250973295 });

//FB.getLoginStatus(handleSessionResponse);
 
// handle a session response from any of the auth related calls
//function handleSessionResponse(response) {
// if we dont have a session (which means the user has been logged out, redirect the user)
//if (!response.session) {
//window.location = "127.0.0.1:8000";
//return;
//}
//if we do have a non-null response.session, call FB.logout(),
//the JS method will log the user out
//of Facebook and remove any authorization cookies
//FB.logout(handleSessionResponse);
</script>
<script>
window.onload="FB.logout();"
</script>
{% load account %}
{% load socialaccount %}
{% providers_media_js %}
{% load staticfiles %}
<p>Username={{ fusername }}</p> <p>Email Id={{ femail }}</p> <p>First_Name={{ ffirst_name }}</p> <p>Last_Name={{ flast_name }}</p>

{% user_display user %} <a href="{% url account_logout %}" onclick="FB.logout();">Logout</a>

</body>
</html>

