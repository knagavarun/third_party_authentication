<html>
<head>
            <b><center>profile of various social accounts</center></br></b>
</head>
<body>
{% load account %}
{% load socialaccount %}
	<a href="{% provider_login_url 'google' method='oauth2' %}">Google</a>     <p>Username={{ gusername }}</p> <p>Email Id={{ gemail }}</p> <p>First_Name={{ gfirst_name }}</p> <p>Last_Name={{ glast_name }}</p>
	{{ value|linebreaks }}	
	<a href="{% provider_login_url 'twitter' method='oauth2' %}">Twitter</a>   <p>Username={{ tusername }}</p> <p>EmailId={{ temail }}</p> <p>Name={{ tname }}</p>
	{{ value|linebreaks }}
	<a href="{% provider_login_url 'facebook' method='oauth2' %}">Facebook</a> <p>Username={{ fusername }}</p> <p>Email Id={{ femail }}</p> <p>First_Name={{ ffirst_name }}</p> <p>Last_Name={{ flast_name }}</p>
</body>
</html>
